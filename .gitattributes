# Keep line endings sane per platform so the launchers run correctly
*.bat text eol=crlf
*.command text eol=lf
*.sh text eol=lf
# CI runs the shell blocks inside these on Linux; a stray CR breaks them
*.yml text eol=lf
*.yaml text eol=lf
